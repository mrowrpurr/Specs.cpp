#define SPEC_GROUP CLI_Options_Version

#include "SpecHelper.h"  // IWYU pragma: keep

StartTestGroup("unit test") {
    It("prints the current version of Specs") {
        // CLI::Options::VersionOption versionOption;
        // versionOption
    }
}
EndTestGroup();

StartTestGroup("integration test") {
    It("prints the current version of Specs") {
        //
    }
}
EndTestGroup();
