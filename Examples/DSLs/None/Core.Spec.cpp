// #include <Specs/DSL/Components/Functions/DefineTest.h>
#include <Specs/DSL/Components/Functions/Print.h>

int main() {
    Specs::Group specs;  // <------

    SpecTestGroup / -- > DSL Group || Fixture SpecTestCase / -- > DSL Spec ||
        Test

            /**
             * Does a 'Run' or 'SpecRun' combine a Spec --> SpecResult? And hold
             * the Promise????
             */

                Specs::App app;

    // Specs::DSL::Components::Functions::Print("HELLO!");
    // Specs::DSL::Components::Functions::DefineTest("This is a test!");

    return 0;
}